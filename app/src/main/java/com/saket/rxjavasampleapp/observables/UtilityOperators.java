package com.saket.rxjavasampleapp.observables;

/**
 * Created by sshriwas on 2020-03-15
 */
public class UtilityOperators {

    //ObserveOn -
    /*
    specifies the scheduler on which an observer will observe this Observable.
    By default, an Observable along with the operator chain will operate on the same thread
    on which its Subscribe method is called.
     */

    //SubscribeOn

    //Do
    //few examples -
    //doOnNext - used to override onNext from emitter
    //doAfterNext - used to perform some action after onNext is invoked - refer interval operator from createObservables.
}
